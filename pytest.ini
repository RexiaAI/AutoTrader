[pytest]
testpaths = tests
markers =
    integration: requires real external services (IBKR, PostgreSQL, OpenAI)
    order: ordering marker used by integration tests (requires pytest-order)
addopts = -m "not integration"


