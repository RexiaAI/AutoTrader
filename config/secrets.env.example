# Copy this file to `config/secrets.env` and fill in your values.
#
# IMPORTANT: Never commit `config/secrets.env` (it is ignored via `.gitignore`).

# ---- AI provider (OpenAI-compatible) ----
# Option A (OpenAI):
OPENAI_API_KEY=your_openai_api_key_here
#
# Option B (local Ollama, OpenAI-compatible):
# OPENAI_BASE_URL=http://localhost:11434/v1
# OPENAI_API_KEY=ollama

# ---- Database (recommended: PostgreSQL) ----
# If this is not set, AutoTrader will fall back to local SQLite (`autotrader.db`).
AUTOTRADER_DATABASE_URL=postgresql://autotrader:autotrader@127.0.0.1:5432/autotrader

# ---- Optional operational overrides ----
AUTOTRADER_BROKER_HOST=127.0.0.1
AUTOTRADER_BROKER_PORT=7497
AUTOTRADER_BROKER_CLIENT_ID=10
AUTOTRADER_AI_MODEL=gpt-4.1-mini
AUTOTRADER_CYCLE_INTERVAL_SECONDS=300

# ---- Postgres tuning (optional) ----
AUTOTRADER_PG_CONNECT_TIMEOUT_SECONDS=3
AUTOTRADER_PG_POOL_MIN=1
AUTOTRADER_PG_POOL_MAX=25


